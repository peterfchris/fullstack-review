select * from users     -- select everything from users
where email = ${email}  -- where the email on the users 
                        -- table is the same as the email 
                        -- that was entered 